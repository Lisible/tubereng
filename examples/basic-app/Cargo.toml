[package]
name = "basic-app"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
tubereng = { path = "../.." }
env_logger = "0.10"
pollster = "0.3"
egui = "0.23"
